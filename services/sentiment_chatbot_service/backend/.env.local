# Local Development Configuration (without Docker)
# Use this when running backend directly on your machine

# MongoDB Configuration (use localhost or your MongoDB Atlas URI)
MONGO_URI=mongodb+srv://research:user@cluster0.sc4zaj7.mongodb.net/research_db?appName=Cluster0
DB_NAME=research_db

# Ollama LLM Configuration (local installation)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_TEMPERATURE=0.2

# API Configuration
API_PORT=8001
